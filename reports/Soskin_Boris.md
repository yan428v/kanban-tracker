# 📄 Отчет о работе над проектом Kanban Tracker

## 1. Моя роль в проекте

**Backend-разработчик, team-lead**:
- Инициализация и архитектура проекта
- Разработка системы аутентификации
- Настройка инструментов качества кода
- Создание изолированной тестовой среды

---

## 2. Выполненные задачи

### 2.1. Инициализация проекта и настройка инфраструктуры

**Создан базовый скелет FastAPI приложения** с многослойной архитектурой:

```
services/main-app/
├── src/
│   ├── api/v1/          # REST API эндпоинты
│   ├── models/          # SQLAlchemy модели
│   ├── repositories/    # Слой работы с БД
│   ├── services/        # Бизнес-логика
│   ├── schemas/         # Pydantic схемы
│   └── core/            # Конфигурация
├── alembic/             # Миграции БД
├── tests/               # Тесты
└── pyproject.toml       # Poetry конфигурация
```

**Настроены технологии:**
- **Poetry** — управление зависимостями
- **Dynaconf** — управление конфигурацией
- **Alembic** — миграции базы данных
- **PostgreSQL + asyncpg** — асинхронная работа с БД
- **SQLAlchemy 2.0** — ORM с async поддержкой
- **Docker Compose** — контейнеризация сервисов

---

### 2.2. Внедрение стандартов качества кода (PEP 8)

**Установлены инструменты автоматического контроля:**

| Инструмент | Назначение |
|------------|------------|
| **Ruff** | Линтер и форматтер (замена black, flake8, isort) |
| **MyPy** | Статический анализ типов |
| **Pre-commit** | Git-хуки для автоматических проверок |

**Результат:**
- ✅ Отформатировано 70+ файлов проекта
- ✅ Исправлены все ошибки PEP 8
- ✅ Настроены автоматические проверки при каждом коммите

---

### 2.3. Разработка модуля JWT-аутентификации

**Реализована полноценная система авторизации на основе JWT токенов.**

#### Архитектура Auth модуля

```
┌─────────────────────────────────────────────────┐
│  JWT Authentication Flow                        │
│                                                 │
│  1. POST /auth/register                         │
│     → Валидация пароля (8+ символов, цифры,    │
│        заглавные, строчные, спецсимволы)        │
│     → Bcrypt хэширование                        │
│     → Создание пользователя                     │
│     → Генерация Access + Refresh токенов        │
│                                                 │
│  2. POST /auth/login                            │
│     → Проверка email + пароля                   │
│     → Генерация новой пары токенов              │
│                                                 │
│  3. POST /auth/refresh                          │
│     → Валидация refresh token                   │
│     → Проверка в БД (не отозван, не истёк)      │
│     → Новый access token (reuse стратегия)      │
│                                                 │
│  4. POST /auth/logout                           │
│     → Отзыв refresh token в БД                  │
│                                                 │
│  5. GET /auth/me                                │
│     → Получение текущего пользователя из JWT    │
└─────────────────────────────────────────────────┘
```

#### Технические решения

**1. Двойная токенизация:**
- **Access Token** (30 минут) — для API запросов
- **Refresh Token** (30 дней) — для обновления access token
- Таблица `refresh_tokens` в БД для механизма отзыва

**2. Безопасность:**
- **Пароли:** Bcrypt хэширование (необратимое)
- **Токены:** JWT подпись с SECRET_KEY (HS256)
- **Валидация:** регулярные выражения для паролей
- **Email:** Pydantic EmailStr валидация

**3. Структура токенов:**
```json
Access Token: {
  "sub": "user-uuid",
  "type": "access",
  "exp": 1234567890
}

Refresh Token: {
  "sub": "user-uuid", 
  "type": "refresh",
  "jti": "unique-token-id",
  "exp": 1234567890
}
```

**4. OAuth2 совместимость:**
- Эндпоинт `/auth/token` для OAuth2PasswordBearer
- Автоматическая авторизация в Swagger UI

#### Реализованные эндпоинты

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/api/v1/auth/register` | Регистрация нового пользователя |
| POST | `/api/v1/auth/login` | Вход (JSON) |
| POST | `/api/v1/auth/token` | Вход (OAuth2 для Swagger) |
| POST | `/api/v1/auth/refresh` | Обновление access token |
| POST | `/api/v1/auth/logout` | Выход (отзыв refresh token) |
| GET | `/api/v1/auth/me` | Получение текущего пользователя |

#### Модели данных

```python
User:
  - id: UUID (PK)
  - name: str
  - email: str (UNIQUE)
  - hashed_password: str
  - created_at, updated_at

RefreshToken:
  - id: UUID (PK)
  - user_id: UUID (FK → users.id)
  - token_jti: str (UNIQUE, indexed)
  - expires_at: datetime
  - is_revoked: bool
```

---

### 2.4. Создание изолированной тестовой среды

**Задача:** Создать E2E тесты для Auth API с полной изоляцией от других компонентов системы.

**Решение: Testcontainers + универсальный conftest**

#### Архитектура тестирования

```
tests/
├── conftest.py                 # Универсальные фикстуры
├── auth/                       # Auth тесты (66 тестов)
│   ├── test_auth_register.py   (18 тестов)
│   ├── test_auth_login.py      (11 тестов)
│   ├── test_auth_token.py      (9 тестов)
│   ├── test_auth_me.py         (10 тестов)
│   ├── test_auth_refresh.py    (10 тестов)
│   └── test_auth_logout.py     (8 тестов)
└── ...                         # Другие тесты
```

#### Ключевые технические решения

**Testcontainers для изоляции:**

```python
@pytest.fixture(scope="module")  # КРИТИЧНО для производительности!
def postgres_container():
    with PostgresContainer("postgres:16") as postgres:
        yield postgres  # Один контейнер на весь тестовый файл
```

- Каждый тестовый модуль получает свою чистую БД в Docker контейнере
- Никаких зависимостей от запущенного сервера
- Полная изоляция между запусками

---

## Использованные технологии и инструменты

### Backend Framework
- **FastAPI 0.121** — асинхронный веб-фреймворк
- **Python 3.13** — язык программирования
- **Poetry** — менеджер зависимостей
- **Pydantic 2.12** — валидация данных

### Database
- **PostgreSQL 16** — реляционная БД
- **SQLAlchemy 2.0** — ORM с async поддержкой
- **Asyncpg** — асинхронный драйвер PostgreSQL
- **Alembic** — миграции схемы БД

### Security
- **python-jose** — работа с JWT токенами
- **Passlib + Bcrypt** — хэширование паролей
- **OAuth2PasswordBearer** — стандарт OAuth2

### Testing
- **Pytest 8.4** — фреймворк тестирования
- **Pytest-asyncio** — поддержка async тестов
- **Httpx** — асинхронный HTTP клиент
- **Testcontainers** — Docker контейнеры для тестов

### DevOps & Quality
- **Docker + Docker Compose** — контейнеризация
- **Ruff** — линтер и форматтер
- **MyPy** — статический анализ типов
- **Pre-commit** — Git хуки для проверок
- **Dynaconf** — управление конфигурацией

---


### Достижения

**1. Архитектурные решения**
- ✅ Создал модульную архитектуру проекта с разделением ответственности
- ✅ Реализовал production-ready JWT аутентификацию с механизмом отзыва токенов
- ✅ Спроектировал систему тестирования с полной изоляцией

**2. Автоматизация**
- ✅ Внедрил автоматические проверки качества кода (pre-commit hooks)
- ✅ Настроил изолированные тесты с testcontainers (не требуют запущенного сервера)

**3. Качество кода**
- ✅ Покрыл Auth API документацией в Swagger UI
- ✅ Написал 66 E2E тестов с понятными docstrings
- ✅ Обеспечил соответствие стандартам PEP 8

---

### Трудности и их решение

**1. Async/Event Loop конфликты в pytest**

*Проблема:* Pytest-asyncio, testcontainers и SQLAlchemy async конфликтовали из-за разных scopes fixtures.

*Решение:*
- Нашёл баланс: module scope для контейнера + function scope для event loop

*Чему научился:*  понимание pytest fixtures, async/await в тестах, lifecycle управление.

---

**2. Оптимизация производительности тестов**

*Проблема:* При `scope="function"` каждый тест создавал свой Docker контейнер — 105 секунд на 63 теста.

*Решение:*
- Использовал `scope="module"` для PostgreSQL контейнера
- Один контейнер переиспользуется для всех тестов в файле
- Между тестами — rollback транзакций вместо пересоздания БД

*Чему научился:* Баланс между изоляцией и производительностью, оптимизация Docker lifecycle.

---

### Общие впечатления от командной работы

**Положительные моменты:**

✅ **Разделение ответственности** — чёткое распределение задач позволило работать параллельно без конфликтов.

✅ **Универсальность решений** — созданная система тестирования поддерживает разные стратегии (изолированные контейнеры + тесты с реальным сервером).

✅ **Обмен опытом** — обсуждение технических решений и структуры проекта.

---

## Заключение

В результате работы создан полнофункциональный модуль аутентификации с JWT токенами, настроена инфраструктура проекта, внедрены стандарты качества кода и построена изолированная тестовая среда. Проект готов к дальнейшей разработке и расширению функционала.

**Итоговые метрики:**
- 🏗️ Создана многослойная архитектура проекта
- 🔐 Реализовано 6 эндпоинтов JWT аутентификации
- ✅ Написано 66 E2E тестов с полным покрытием сценариев
- 🚀 Производительность тестов: 10-15 секунд для 63 тестов
- 📝 Настроена автопроверка кода